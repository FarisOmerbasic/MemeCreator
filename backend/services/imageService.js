const sharp = require('sharp')

function buildOverlay(width, height, params) {
    const {
    topText = '',
    bottomText = '',
    fontfamily = 'Impact',
    fontSize = 64,
    textColor = 'FFFFFF',
    strokeColor = '#000000',
    strokeWidth = 3,
    textAlign = 'center',
    padding = 20,
    allCaps = true,
    watermarkImage,
    watermarkPosition = 'bottom-right'
    } = params

}